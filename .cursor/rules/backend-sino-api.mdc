---
description: Shioaji (永豐金 Sino API) 連線規範：Singleton 登入、Rate Limiting 快取、持倉合併、全市場 Event-driven 排程
globs: backend/**/*.py
alwaysApply: false
---

# Sino API 規範

## 1. 連線單例 (Singleton)

Shioaji 不支援多程式同時 Login。使用 FastAPI `lifespan` 做全域單一初始化：

```python
# ✅ 正確：lifespan 單例
from contextlib import asynccontextmanager
import shioaji as sj

_api: sj.Shioaji | None = None

@asynccontextmanager
async def lifespan(app):
    global _api
    _api = sj.Shioaji()
    _api.login(api_key=API_KEY, secret_key=SECRET_KEY, fetch_contract=True)
    yield
    _api.logout()

# ❌ 錯誤：在每個路由或每次請求內呼叫 login()
```

## 2. 登入機制

- 金鑰從 `.env` 讀取：`SJ_API_KEY`、`SJ_SEC_KEY`。
- 登入時必須帶 `fetch_contract=True` 確保商品檔載入。

## 3. Rate Limiting — 必做 TTL 快取

行情查詢限制：5 秒內 50 次。對 Snapshot / Kbars 端點套 TTL 快取：

```python
# ✅ 正確：cachetools TTL 快取
from cachetools import TTLCache
_snapshot_cache: TTLCache = TTLCache(maxsize=1, ttl=10)

def get_snapshots():
    if "data" not in _snapshot_cache:
        _snapshot_cache["data"] = api.snapshots(...)
    return _snapshot_cache["data"]

# ❌ 錯誤：每次請求直接呼叫 api.snapshots(...)
```

## 4. 持倉合併 (整股 + 零股)

永豐 API 將整股 `Unit.Common` 與零股 `Unit.Share` 分開回傳，後端需合併同一股票後再回傳：

```python
from shioaji.constant import Unit

positions = api.list_positions(api.stock_account, unit=Unit.Common)
odd_positions = api.list_positions(api.stock_account, unit=Unit.Share)
# 以 code 為 key 合併 quantity / pnl，再序列化給前端
```

## 5. 全市場資料：Event-driven 排程

APScheduler 每 1–3 分鐘背景抓全市場 Snapshot，存入記憶體 dict 或 SQLite。
前端呼叫 `/api/market/treemap` 時永遠只讀快取，禁止直接觸發 Shioaji 請求。
