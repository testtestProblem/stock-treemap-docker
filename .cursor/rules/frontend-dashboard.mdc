---
description: 前端 Dashboard 套件用途、版面規範、Treemap 規則與漲跌顏色定義
globs: frontend/**/*.{ts,tsx,js,jsx,css}
alwaysApply: false
---

# 前端 Dashboard 規範

## 套件職責分工

| 套件 | 用途 |
|------|------|
| React 19 | 元件狀態與生命週期 |
| Vite | 開發伺服器與生產打包 |
| Tailwind CSS v4 | 以 `@import "tailwindcss";` 引入，全 CSS-driven 設定 |
| D3.js | 僅負責 treemap 幾何運算（`d3.hierarchy`、`d3.treemap`） |
| Recharts | 折線圖、績效比較圖（SVG） |
| Motion (motion/react) | 頁面轉場與卡片動畫（Spring / AnimatePresence） |
| Lucide React | 所有 icon 統一使用此庫 |

> 不要用 D3 做 DOM 操作；幾何計算後交給 React 渲染 SVG。

## Dashboard 版面結構

```
┌─────────────────────────────────────────────┐
│  頂部資訊卡 ×4                               │
│  真實總資產 | 帳戶餘額 | 持倉市值 | 待交割款  │
├──────────────┬──────────────────────────────┤
│  持倉列表    │  Treemap（主區塊）            │
│  (左側表格)  │  [全台股] [自選清單] 切換按鈕  │
├──────────────┴──────────────────────────────┤
│  資產績效比較折線圖（我的資產 / 0050 / 2330）│
└─────────────────────────────────────────────┘
```

## 績效比較圖

- 使用 Recharts `LineChart`。
- 三條曲線均**標準化為相對百分比**（第一天 = 0%），才能在同一圖表比較。

## Treemap 規範

- 板塊顯示：`股票代號`、`公司名稱`、`漲跌幅 %`。
- 支援「全台股」與「自選清單」切換（對應後端 `?mode=market|watchlist`）。

## 漲跌顏色（美式規範）

```
漲（正）→ 綠色  #22c55e (green-500)
跌（負）→ 紅色  #ef4444 (red-500)
平盤     → 灰色  #6b7280 (gray-500)
```

> 注意：美式與台式相反。本專案統一用美式（漲綠跌紅）。
